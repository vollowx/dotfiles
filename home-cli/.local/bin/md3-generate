#!/usr/bin/env sh

mkdir -p ~/.cache/dotfiles/md3
matugen $@
ln -sf ~/.cache/dotfiles/md3/dunst-colors.conf ~/.config/dunst/dunstrc.d/99-md3-colors.conf

update-colors-term
update-colors-nvim
qutebrowser :config-source
swaymsg reload
